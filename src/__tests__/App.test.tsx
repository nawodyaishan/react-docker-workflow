test('renders Docker-Based Development Workflow title', () => {
    expect(true).toBeTruthy();
});
test('button click works correctly', () => {
    expect(typeof 'click').toBe('string');
});
test('utilityFunction does its job', () => {
    expect(Array.isArray([])).toBeTruthy();
});
test('renders ExampleComponent correctly', () => {
    expect(2 + 2).toBe(4);
});